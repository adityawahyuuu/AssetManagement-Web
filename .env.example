# ==================== AssetManagement Web - Environment Configuration ====================
#
# This is an example environment file. Copy this to .env.local and configure your values.
# IMPORTANT: Never commit .env.local to version control!
#
# Usage:
#   1. Copy this file: cp .env.example .env.local
#   2. Update the values in .env.local with your actual configuration
#   3. Restart your development server
#

# ==================== Application Configuration ====================

# Node environment (development, production, test)
NODE_ENV=development

# ==================== API Configuration ====================

# Backend API Base URL
# REQUIRED: Must be set to your AssetManagement-API backend URL
#
# Local Development Options:
# - HTTP (typical):     http://localhost:5080
# - HTTPS (if setup):   https://localhost:5080
#
# Production (REQUIRED to use HTTPS):
# - HTTPS only:         https://api.yourdomain.com
#
# SECURITY: Production MUST use HTTPS to protect credentials and data in transit
NEXT_PUBLIC_API_URL=http://localhost:5080

# ==================== Authentication Configuration ====================

# Token expiration time in minutes
# Default: 10 minutes
# Recommendation: Keep short for security (5-15 minutes)
NEXT_PUBLIC_TOKEN_EXPIRATION_MINUTES=10

# ==================== Pagination Configuration ====================

# Default number of items per page
# Default: 10
# Range: 1-100
NEXT_PUBLIC_DEFAULT_PAGE_SIZE=10

# ==================== Feature Flags (Optional) ====================

# Enable debug mode (shows additional logs)
NEXT_PUBLIC_DEBUG_MODE=false

# Enable analytics
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# ==================== External Services (Optional) ====================

# Add any external service configurations here
# Example: NEXT_PUBLIC_SENTRY_DSN=
# Example: NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=

# ==================== Notes ====================
#
# Environment Variable Naming Convention:
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Variables without NEXT_PUBLIC_ are only available server-side
# - Never put sensitive secrets in NEXT_PUBLIC_* variables
#
# Security Best Practices:
# - Never commit actual credentials or sensitive data
# - Use different .env.local files for different environments
# - Rotate secrets regularly
# - Use secure HTTPS URLs in production
#
# For more information: https://nextjs.org/docs/basic-features/environment-variables
